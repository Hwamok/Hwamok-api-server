spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${db.url:jdbc:mysql://localhost:3306/hwamoktest}
    username: ${db.username:root}
    password: ${db.password:1234}
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: true

jwt:
  secretKey: hwamokJwtSecretKey20231102PM1057

slack:
  bot-token: ${bot.token:1234}
  log-channel: ${bot.channel:1234}

cloud:
  aws:
    credentials:
      access-key: AKIA5CD3QQSFA2EMCR5N
      secret-key: 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
    region:
      static: ap-northeast-2

s3:
  bucket: hwamok3
